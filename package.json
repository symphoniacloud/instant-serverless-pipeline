{
  "name": "instant-serverless-pipeline",
  "version": "1.0.0",
  "description": "Skeleton project for a continuously deployable serverless app",
  "scripts": {
    "clean": "rm -rf node_modules target source-dist outputfile.txt package-lock.json packaged-tmplate.yaml",
    "test": "mocha",
    "dist": "mkdir target ; zip -r target/lambda lib node_modules"
  },
  "devDependencies": {
    "mocha": "^3.5.0"
  },
  "dependencies": {
    "request": "^2.83.0"
  }
}
